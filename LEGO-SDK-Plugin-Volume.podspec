Pod::Spec.new do |s|

  s.name         = "LEGO-SDK-Plugin-Volume"
  s.version      = "0.0.1"
  s.summary      = "A short description of LEGO-SDK-Plugin-Volume."
  s.description  = <<-DESC
                   DESC
  s.homepage     = "http://EXAMPLE/LEGO-SDK-Plugin-Volume"
  s.license      = "MIT (example)"
  s.author             = {}
  s.platform     = :ios, "8.0"
  s.source       = { :git => "http://EXAMPLE/LEGO-SDK-Plugin-Volume.git" }
  s.source_files  = "ios/Source/*.{h,m}"
  s.requires_arc = true

end
